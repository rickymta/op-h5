# Header chung cho moi location proxy sang Adapter.
# X-Forwarded-For phai co: Adapter dung no de biet IP that cua nguoi choi (chong do
# mat khau, gioi han theo IP). Adapter chi tin header nay khi request den tu loopback.
proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_http_version 1.1;
proxy_read_timeout 30s;
